*.pdb

# 2026-09-09 — 패키징 결과물은 커밋하지 않는다.
#   예전에 Client/Windows 와 Server/WindowsServer 를 통째로 넣었다가 이력에서 뺐다.
#   빌드 산출물은 소스가 아니고, 한 번 들어가면 이력 재작성 없이는 못 지운다.
/Client/
/Server/
Binaries/
Intermediate/
DerivedDataCache/
Saved/
Build/
*.pdb
